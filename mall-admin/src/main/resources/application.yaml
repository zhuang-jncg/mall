server:
  port: 8080
spring:
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  datasource:
    url: jdbc:mysql://localhost:3306/mall?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: root
    druid:
      initial-size: 5 #????????
      min-idle: 10 #???????
      max-active: 20 #?????
      web-stat-filter:
        exclusions: "*.js,*.gif,*.jpg,*.png,*.css,*.ico,/druid/*" #?????????
      stat-view-servlet: #???????????????
        login-username: druid
        login-password: druid
  data:
    redis:
      host: 192.168.131.146 # Redis?????
      database: 0 # Redis?????????0?
      port: 6379 # Redis???????
      password:  123321 # Redis?????????????
      timeout: 3000ms # ??????????
  servlet:
    multipart:
      enabled: true #??????
      max-file-size: 10MB #?????????10M
mybatis:
  mapper-locations:
    - classpath:dao/*.xml
    - classpath*:com/**/mapper/*.xml
management:
  endpoints:
    web:
      exposure:
        include: '*' #??Actuator?????
  endpoint:
    health:
      show-details: always #?????????
    env:
      show-values: always #??Actuator???????****?????????
    configprops:
      show-values: always #??Actuator???????****?????????
    info:
      enabled: true
redis:
  database: mall-swarm
  key:
    admin: 'ums:admin'
  expire:
    common: 86400 # 24??
aliyun:
  oss:
    endpoint: oss-cn-shenzhen.aliyuncs.com # oss?????????
    accessKeyId: test # ?????????????
    accessKeySecret: test # ????????????oss????????????
    bucketName: macro-oss # oss?????
    policy:
      expire: 300 # ?????(S)
    maxSize: 10 # ??????(M)
    callback: http://39.98.190.128:8080/aliyun/oss/callback # ????????????
    dir:
      prefix: mall/images/ # ?????????
minio:
  endpoint: http://locahost:9000 #MinIO??????
  bucketName: mall #?????
  accessKey: minioadmin #???key
  secretKey: minioadmin #?????
springdoc:
  swagger-ui:
    # ??Swagger UI??
    path: /swagger-ui.html
    # ??Swagger UI??
    enabled: true
    # ????tag?operation???????????????
    doc-expansion: 'none'
  api-docs:
    # ??api-docs??
    path: /v3/api-docs
    # ??api-docs
    enabled: true
  group-configs:
    - group: 'default'
      packages-to-scan: com.macro.mall.controller
  default-flat-param-object: false
sa-token:
  # token?? (????cookie??)
  token-name: Authorization
  # token????????-1??????
  timeout: 604800
  # token????? (???????????token??)????
  active-timeout: -1
  # ???????????? (?false?????????)
  is-concurrent: true
  # ?????????????????token (?false?????????token)
  is-share: false
  # token??
  token-style: uuid
  # ????????
  is-log: false
  # ???cookie???token
  is-read-cookie: false
  # ???head???token
  is-read-header: true
  # token??
  token-prefix: Bearer
  # jwt??
  jwt-secret-key: sa-secret-key123
  # ????banner
  is-print: false